indx0 = lastLaser
indx1 = currentLaser
indx2 = nextLaser

dist = dist(pos0, pos1)

angle = absAngle


laserStatus = [state0, state1, state2, state3]

# prüfen ob der Laser ON ist
if laserStatus[indx1] == True:
    

cmdLine = command + F + X + Y + E + A



